CREATE TABLE students (
    id INTEGER,
    student_name TEXT,
    house TEXT,
    head TEXT,
    PRIMARY KEY(id),
    FOR KEY
);

CREATE TABLE houses {
    id INTEGER,
    house_name TEXT,
    PRIMARY KEY(id),
}